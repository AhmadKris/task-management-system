{
  "name": "task-management-system",
  "private": true,
  "description": "Task Management System - fullstack (Express + React + MySQL)",
  "scripts": {
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "test": "npm --prefix backend test && npm --prefix frontend test",
    "lint": "npm --prefix backend run lint && npm --prefix frontend run lint",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.6",
    "lint-staged": "^15.2.10",
    "prettier": "^3.9.6"
  },
  "lint-staged": {
    "backend/**/*.js": "prettier --ignore-unknown --write",
    "frontend/**/*.{js,jsx,css}": "prettier --ignore-unknown --write"
  }
}
